# - name: Get short SHA from tag
#   command: git rev-parse --short=8 "{{ CI_TAG }}^{commit}"
#   args:
#     chdir: "{{ PROJECT_URL }}"
#   register: CI_COMMIT_SHORT_SHA
#   changed_when: false

# - name: Show short SHA for tag
#   debug:
#     msg: "Tag {{ CI_TAG }} corresponds to commit short SHA {{ CI_COMMIT_SHORT_SHA.stdout }}"

- name: Récupérer le vrai SHA du tag (compatible annotated tag)
  command: >
    git ls-remote --tags {{ PROJECT_URL }} "{{ CI_TAG }}^{}"
  register: tag_info

- name: Extraire le short SHA (8 caractères)
  set_fact:
    CI_COMMIT_SHORT_SHA: "{{ tag_info.stdout.split()[0][:8] }}"

- name: Afficher le short SHA
  debug:
    msg: "Short SHA du tag {{ CI_TAG }} = {{ CI_COMMIT_SHORT_SHA }}"



- name: "Installer Node.js via NVM"
  block:
    - name: Installer les dépendances nécessaires
      apt:
        name:
          - curl
          - git
        state: present
        update_cache: yes
      become: true

    - name: Créer le répertoire .nvm
      file:
        path: "/home/admin/.nvm"
        state: directory
        owner: admin
        group: admin
        mode: '0755'
      become: true

    - name: Installer NVM et Node.js 
      shell: |
        {{ nvm_load }}

        # Installer NVM si absent

        if [ ! -s "$NVM_DIR/nvm.sh" ]; then
          curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
        fi

        # Recharger NVM pour s'assurer qu'il est bien disponible

        {{ nvm_load }}

        # Installer Node.js et le définir par défaut
        nvm install {{ node_version }} || true
        nvm alias default {{ node_version }}
        nvm use default
      args:
        executable: /bin/bash
      become: true
      become_user: admin
      environment:
        HOME: /home/admin
      register: node_install

- name: Ensure Git is installed
  ansible.builtin.package:
    name: git
    state: present

- name: "Préparer les répertoires de cache pour admin"
  file:
    path: "{{ item }}"
    state: directory
    owner: admin
    group: admin
    mode: '0755'
  become: true
  loop:
    - "/home/admin/.cache"
    - "/home/admin/.npm"
    - "/home/admin/.config"

- name: "Supprimer le répertoire de build s'il existe"
  file:
    path: "{{ build_dir }}"
    state: absent
  become: true

- name: "Créer un répertoire de build vide"
  file:
    path: "{{ build_dir }}"
    state: directory
    owner: admin
    group: admin
    mode: '0755'
  become: true

- name: "Clone du repository"
  git:
    repo: "https://oauth:{{ TOKEN }}@{{ REGISTRY_HOST }}/{{ PROJECT_URL }}.git"
    dest: "{{ build_dir }}"
    version: "{{ CI_COMMIT_SHORT_SHA }}"
    force: true
  become: true
  become_user: admin
  register: git_clone_result

- name: "Affichage du commit utilisé"
  debug:
    msg: "Commit utilisé: {{ CI_COMMIT_SHORT_SHA}} ({{ git_clone_result.after }})"

- name: "Création d'un fichier vide avec le nom du commit"
  file:
    path: "{{ build_dir }}/.{{ CI_COMMIT_SHORT_SHA}}."
    state: touch
    owner: admin
    group: admin
    mode: '0644'
  become: true

- name: "Vérification du fichier package.json"
  stat:
    path: "{{ build_dir }}/package.json"
  register: package_json

- name: "Installation des dépendances NPM"
  shell: |
    {{ nvm_load }}
    nvm use {{ node_version }}
    
    # Nettoyer le cache npm
    npm cache clean --force 2>/dev/null || true
    
    # Installer les dépendances
    npm install --omit=dev
  args:
    executable: /bin/bash
    chdir: "{{ build_dir }}"
  become: true
  become_user: admin
  environment:
    HOME: /home/admin
  when: package_json.stat.exists

- name: "Suppression des fichiers inutiles pour le déploiement"
  file:
    path: "{{ build_dir }}/{{ item }}"
    state: absent
  loop:
    - ".git"
    - ".gitignore"
    - "README.md"
  ignore_errors: true

- name: "Création de l'archive complète du projet"
  archive:
    path: "{{ build_dir }}/"
    dest: "{{ build_dir }}/{{ project_archive }}"
    format: gz
    exclude_path:
      - "{{ build_dir }}/{{ project_archive }}"

- name: "Vérification de l'archive créée"
  stat:
    path: "{{ build_dir }}/{{ project_archive }}"
  register: project_archive_stat

- name: "Message de succès"
  debug:
    msg: "✅ Archive du projet créée : {{ project_archive_stat.stat.path }}"
  when: project_archive_stat.stat.exists

- name: "Message d'erreur si archive absente"
  fail:
    msg: "❌ L'archive du projet n'a pas pu être créée !"
  when: not project_archive_stat.stat.exists