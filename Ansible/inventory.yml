all:
  children:
    local_build:
      hosts:
        localhost:
          ansible_connection: local

    node_server:
      hosts:
        raspberry1:
          ansible_host: 192.168.1.39
          ansible_user: admin